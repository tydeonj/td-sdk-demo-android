# TD Android SDK 接入指南（三步）

完整对接文档：https://tydeonj.github.io/td-sdk-docs/  
iOS Demo：https://github.com/tydeonj/td-sdk-demo-ios

广告位只改 [`app/src/main/java/com/td/sdkdemo/AdConfig.java`](app/src/main/java/com/td/sdkdemo/AdConfig.java)（本 Demo：`appId=31014`，插屏 621314 / Banner 621311 / 开屏 621313 / 原生 621312 / 激励 621315）。iOS 对外是另一套：`appId=31016` / 开屏 621301 / 激励 621305 / 插屏 621304 / Banner 621302 / 原生 621303。

---

## 1. 集成

正式工程用 Maven Central 拉 TD。广告源按后台实际配置加，没接的不要加。完整对照见 [02 Android 集成](https://tydeonj.github.io/td-sdk-docs/02_Android集成)。

本 Demo 用 Maven Central 上的 `com.tyedo:*:1.1.2.7`，debug/release **开 R8**，规则见 [`app/proguard-rules.pro`](app/proguard-rules.pro) / 对接文档 [02 §6 混淆](https://tydeonj.github.io/td-sdk-docs/02_Android集成)。

**必加（TD）**

仓库：`google()`、`mavenCentral()`。

```gradle
def tdVer = '1.1.2.7'
implementation "com.tyedo:td-ads-base:${tdVer}"
implementation "com.tyedo:td-ads-sdk:${tdVer}"
```

**按需（接了哪个平台加哪一段）**

| 平台 | 额外仓库 | 依赖 |
| --- | --- | --- |
| JinDai | 无，官方 AAR 放 `libs/` | `com.tyedo:jdsdk_ads` + `files('libs/YDSDK-release.aar')` |
| AdGain | 按 AdGain 官方文档配置私有 Maven（账号向对方申请，不要写进工程） | `com.tyedo:adgain_ads` + `com.adgain:adgain-sdk:4.1.5` |
| LiteMob | `https://hub.litemob.com/api/v4/projects/2/packages/maven` | `com.tyedo:ltmb_ads` + `com.ltmb.ltsdk:core:2.9.5`，以及官方要求的 Glide / Gson / OkHttp / CardView |

---

## 2. Init

本 Demo 首页不展示合规开关。点「初始化 SDK」会按默认同意调用隐私 API，方便看广告流程。

**正式 App 必须先让用户同意自己的隐私政策，再 Init。** 对照 https://tydeonj.github.io/td-sdk-docs/隐私合规 。

```java
TDAdsSDK.setDebugMode(true); // 对外调试开关，给接入方开发者调用；正式上线请关
TDAdsSDK.setAuthUID(context, true); // 允许采 OAID；SDK 默认关。正式 App 按用户授权。JinDai / AdGain 无 OAID 不出广告，见 [04](https://tydeonj.github.io/td-sdk-docs/04_初始化与隐私)
TDAdsSDK.setOpenPersonalizedAd(true); // 默认开、不持久化
TDAdsSDK.initSdk(context, "1", new TDInitListener() {
    @Override public void onSuccess() { }
    @Override public void onFailed(TDError error) { }
});
```

Init 成功后 Logcat 滤 `TDIntegration` 看集成检测（不铺在首页）：

- `adapter … LINKED`：TD 桥已编进包
- `peer … LINKED`：对方 SDK 入口类已在运行时
- `missing`：没加对应 AAR / 对方包。这是 no_fill 之前先看的

`setDebugMode(true)` 后，某个源的加载看它自己的 TAG（类短名）。Logcat 滤 `JdsdkReward` 能看到 `bind` / `load start` / `load ok|fail` 就是进了京东激励 Adapter；**一条没有** = Config 没下发 / className 对不上 / 模块没编进包。Init 滤 `JdsdkInit`。其它源同理：`AdgainReward`、`LtmbReward`。iOS 类名带 `TD` 前缀，搜 `TDJdsdkReward`。

---

## 3. 按样式抄文件

Android Demo 五种样式在一个 [`FormatActivity.java`](app/src/main/java/com/td/sdkdemo/FormatActivity.java) 里，按 `type` 分支。抄对应 `createAd` / `load` / `show` 段即可。iOS 同样是一个 `FormatViewController`。完整 API 表与示例见 [05](https://tydeonj.github.io/td-sdk-docs/05_激励视频)～[09](https://tydeonj.github.io/td-sdk-docs/09_原生广告)。

| type | 广告 | 类 |
|------|------|-----|
| `reward` | 激励 | `TDReward` |
| `interstitial` | 插屏 | `TDInterstitial` |
| `splash` | 开屏 | `TDSplash`（Show 带全屏 container） |
| `banner` | Banner | `TDBanner` |
| `native` | 原生 | `TDNative`（Load 不自动 Show；自渲染在点 Show 时拼装） |

容器必须按格式抄（否则 ADN 拒 Show，Demo 看不见广告）：

| type | 容器 |
|------|------|
| `splash` | 全屏 overlay（`match_parent`），不要 120dp 小盒子；用 `INVISIBLE` 占位 |
| `banner` | 宽 `match_parent`、高 ≥50dp（京东 Banner 自适应，建议 200dp，锁 50dp 会压扁） |
| `native` | ≥250dp；自渲染在 Show **之前**拼 `td_title` / `td_desc` / `td_cta` / `td_image`（`imageUrl`）。SDK Load 成功不会自动 Show |
| `reward` / `interstitial` | 无容器 |

共通顺序：构造广告位 → `setAdListener` → `loadAd` → `isReady` → `show`。离开页面 `onDestroy`。

请求尺寸：未传时用格式默认（Banner 屏宽×50dp，原生 250dp，开屏整屏）。要改在 Load 前（**像素**）：

```java
banner.setAdSize(1080, 150);          // 或
Map<String, Object> p = new HashMap<>();
p.put("width", 1080);
p.put("height", 150);
banner.setCustomParams(p);
```

容器仍要够大，否则 ADN 会 `ad show size error`。

激励走 LTMB 时，Load 前必须带用户 ID（下游飞鸽展示可能要求非空）。`channel` 等有值也会透传（京东 Android Init 会用）：

```java
Map<String, Object> p = new HashMap<>();
p.put("userId", "your_app_user_id");
p.put("channel", "your_channel");
reward.setCustomParams(p);
```

正式包请换成你们的登录用户 ID。详见 [10 自定义参数与尺寸](https://tydeonj.github.io/td-sdk-docs/10_自定义参数与尺寸)。`sceneId` 会进 TD 埋点；若对接平台要求数字，请传纯数字串。
